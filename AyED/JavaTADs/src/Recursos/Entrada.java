package Recursos;

import java.util.Scanner;

public class Entrada {
    public static final Scanner sc=new Scanner(System.in);
    //Unico atributo de la clase Entrada 
    //publico porque todas las clases lo van a usar
    //static porque ? es de la clase no del objeto? q signiifica
    //final porque no se puede instanciar, ya va instanciado
}